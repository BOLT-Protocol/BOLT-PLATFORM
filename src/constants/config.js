/* eslint-disable no-undef */
// const env = process.env.NODE_ENV;

const prefix = 'https://';
const keystone = 'rest.boltchain.io';
export const keystoneUrl = prefix + keystone;

export const serverUrl = 'https://api.boltchain.io';